apiVersion: v1
kind: Service
metadata:
  name: api-service
  namespace: multi-tier
  labels:
    app: api-service
spec:
  type: LoadBalancer
  selector:
    app: api-service
  ports:
  - port: 80
    targetPort: 5000
    protocol: TCP
    name: http
